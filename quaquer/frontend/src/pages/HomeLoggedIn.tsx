// import { useAuth } from '../contexts/AuthContext';

export const HomeDefault: React.FC = () => {
    // const { token, logout } = useAuth();


    return (
        <div>Home (when logged)</div>
    );
}

export default HomeDefault
